window.biblioApi.quickView();
/* window.biblioApi.carrousel(); */

window.biblioApi.notification('OH no!', 'puto el que lee', 'danger');
console.log(window.biblioApi.Bulma);
/* window.biblioApi.camera().initialize(function (res, msg, err) {
  //notif.show('Notification Title', 'Notification message', 'primary', 2000);
  if (!res) {
    console.error('camera:', msg, err);
    //window.biblioApi.notification().show('OH no!', msg, 'danger');
    return;
  }
}); */
